// next.config.js
module.exports = {
  output: 'export',
  images: {
    unoptimized: true, // Necessary for GitHub Pages to handle images
  },
  // Optional: specify the base path if your project is not at the root
  basePath: '/insurance-blog-website',
};
